import React from 'react';

export const AppFooter = (props) => {

    return (
        <div className="layout-footer !bg-spacecadet">
            by
            <span className="font-medium ml-2">PowerDz</span>
        </div>
    );
}
